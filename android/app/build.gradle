plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.zion.os"
    compileSdk = 34
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17
    }

    defaultConfig {
        applicationId = "com.zion.os"
        minSdk = 21
        targetSdk = 34
        versionCode = 1
        versionName = "4.1.0"
    }

    buildTypes {
        release {
            // Never ship an APK signed with the developer/debug key.
            // Configure a release keystore in a private Gradle properties file
            // or CI secrets before publishing to a store.
            signingConfig = null
            minifyEnabled = false
            shrinkResources = false
        }
    }
}

flutter {
    source = "../.."
}
